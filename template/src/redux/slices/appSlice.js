import { createSlice } from "@reduxjs/toolkit";

const initialAppSlice = {};

const appSlice = createSlice({
  name: "hover",
  initialState: initialAppSlice,
  reducers: {},
});

export const {} = appSlice.actions;
export default appSlice.reducer;
